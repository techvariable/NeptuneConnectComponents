import { Component, Host, h } from '@stencil/core';

@Component({
  tag: 'table-header',
  styleUrl: 'table-header.css',
  shadow: true,
})
export class TableHeader {

  render() {
    return (
      <Host>
        
      </Host>
    );
  }
}
